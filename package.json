{
  "name": "ci-cd-web",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand",
    "build": "cp -r src dist"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
